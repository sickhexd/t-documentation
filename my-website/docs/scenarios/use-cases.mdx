# Пользовательские сценарии и бизнес-логика

## Функциональные требования

| ID | Категория | Требование |
|---|---|---|
| FR-001 – 003 | Авторизация | Регистрация, авторизация и восстановление пароля |
| FR-004 – 006 | Бронирование | Отображение свободных мест, создание брони, предотвращение двойного бронирования |
| FR-007 – 008 | Тарификация | Отображение и выбор тарифов |
| FR-009 – 012 | Оплата | Оплата СБП, обработка статусов, формирование чека |
| FR-013 – 016 | Администрирование | Управление местами, тарифами и история бронирований |
| FR-017 – 018 | Уведомления | Уведомления об изменении статусов брони/оплаты |

## Бизнес-правила

| ID | Правило |
|---|---|
| BR-01 | Email уникален в системе |
| BR-02 | Одно место не может быть забронировано дважды на один и тот же временной интервал |
| BR-03 | При конкурентном бронировании выигрывает первый завершивший транзакцию |
| BR-04 | Тариф должен быть активен |
| BR-05 | Стоимость рассчитывается по формуле, закреплённой в тарифе |
| BR-06 | Один тариф может быть выбран только один раз для одной брони |
| BR-07 | Бронь не считается активной до успешной оплаты |
| BR-08 | Один платёж соответствует одной брони |

## Основные сценарии

### Диаграмма Use Cases

```plantuml
@startuml

actor Пользователь as u
actor "Администратор системы" as a

package "Система бронирования парковки" {
  usecase "Выбрать интервал\nбронирования" as UC1
  usecase "Просмотреть\nсвободные места" as UC2
  usecase "Выбрать место" as UC3
  usecase "Выбрать тариф" as UC4
  usecase "Подтвердить\nбронирование" as UC5
  usecase "Оплатить бронь" as UC6
  usecase "Получить номер\nбронирования" as UC7
  usecase "Управлять\nпарковочными местами" as UC8
  usecase "Управлять\nтарифами" as UC9
  usecase "Просматривать\nбронирования" as UC10
  usecase "Изменять статус\nмест" as UC11
}

u --> UC1
u --> UC3
u --> UC4
u --> UC5
u --> UC6
u --> UC7

a --> UC8
a --> UC9
a --> UC10
a --> UC11

UC3 --> UC2 : include
UC5 --> UC3 : include
UC5 --> UC4 : include
UC5 --> UC6 : include
UC7 <-- UC5 : extend

@enduml
```

### Последовательность взаимодействия

```plantuml
@startuml
skinparam sequenceMessageAlign center

actor Пользователь as User
participant "Клиент\n(браузер/приложение)" as Client
participant "Сервер\n(бэкенд)" as Server
database "БД" as DB
participant "Платёжный\nсервис" as Payment
queue "RabbitMQ" as Broker
participant "Сервис\nсообщений" as Messenger

== Бронирование ==
User -> Client : Вводит интервал и нажимает "Забронировать"
activate Client

Client -> Server : POST /booking {interval, placeId}
activate Server

Server -> DB : SELECT свободные места по интервалу
activate DB
DB --> Server : Список свободных мест
deactivate DB

Server --> Client : 200 OK {places[]}
deactivate Server

Client --> User : Отображает свободные места
deactivate Client

== Выбор места и тарифа ==
User -> Client : Выбирает место и тариф
activate Client

Client -> Server : POST /booking/confirm {placeId, tariff, interval}
activate Server

Server -> DB : SELECT тариф и данные места
activate DB
DB --> Server : Данные тарифа
deactivate DB

Server -> Server : Расчёт стоимости брони
Server --> Client : 200 OK {cost}
deactivate Server

Client --> User : Отображает стоимость
deactivate Client

== Оплата ==
User -> Client : Нажимает "Оплатить"
activate Client

Client -> Server : POST /payment {bookingId, amount}
activate Server

Server -> Payment : POST /charge {amount, cardToken}
activate Payment

alt Платёжная система недоступна
    Payment --> Server : 503 Service Unavailable
    deactivate Payment
    Server --> Client : 503 {error: "Платёжная система недоступна"}
    deactivate Server
    Client --> User : Показывает ошибку
    deactivate Client

else Оплата не прошла
    Payment --> Server : 402 {status: "failed"}
    deactivate Payment
    Server --> Client : 402 {error: "Ошибка оплаты"}
    deactivate Server
    Client --> User : Показывает ошибку
    deactivate Client

else Оплата успешна
    Payment --> Server : 200 OK {status: "success", txId}
    deactivate Payment

    Server -> DB : UPDATE место → "Забронировано"\nINSERT номер бронирования
    activate DB
    DB --> Server : OK
    deactivate DB

    Server -> Broker : publish paymentStatus
    activate Broker
    Broker --> Messenger : paymentStatus
    deactivate Broker
    activate Messenger
    Messenger --> Server : 200 OK
    deactivate Messenger

    Server --> Client : 200 OK {bookingNumber}
    deactivate Server

    Client --> User : Показывает номер бронирования
    deactivate Client
end

@enduml
```

### Ключевые Use Cases

#### UC-4: Бронирование парковочного места
**Предусловия:** Пользователь авторизован. В системе существуют парковочные места.
**Основной поток:**
1. Пользователь выбирает временной интервал.
2. Система отображает список доступных мест.
3. Пользователь выбирает конкретное место и тариф (UC-5).
4. Система проверяет доступность, фиксирует бронь и отображает подтверждение.
**Альтернативные потоки:** Нет доступных мест / Место уже занято.

#### UC-6: Оплата через СБП
**Предусловия:** Бронирование создано, находится в статусе «Ожидает оплаты».
**Основной поток:**
1. Пользователь нажимает «Оплатить».
2. Формируется платёжный запрос, переход на форму оплаты.
3. Платёжная система возвращает результат транзакции.
4. Система фиксирует статус, публикует событие изменения статуса оплаты и отправляет уведомление (UC-8).

#### UC-7: Администрирование системы
**Основной поток:** Авторизация администратора → управление местами (создание/блокировка), тарифами (редактирование/деактивация) и просмотр истории.

## Бизнес-процесс бронирования

**Основной поток BPMN:**
1. Запрос на бронирование → выбор интервала.
2. Проверка доступных вариантов через правила DMN (статусы тарифов).
3. **Шлюз:** Доступны ли варианты?
   - Нет → Отказ.
   - Да → Выбор места и тарифа.
4. Подтверждение бронирования и инициация оплаты.
5. Ожидание результата от платёжной системы.
6. **Шлюз:** Оплата проведена?
   - Нет → Отмена брони.
   - Да → Подтверждение в системе, публикация события в RabbitMQ и отправка уведомления клиенту.

![](/img/process.png)
